# Vercel KV credentials (auto-populated when you link a KV store on Vercel)
# For local development: either run `vercel env pull` or leave empty to use in-memory fallback
KV_REST_API_URL=
KV_REST_API_TOKEN=
